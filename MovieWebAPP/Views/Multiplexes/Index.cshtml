@model IEnumerable<MovieWebAPP.Models.Multiplex>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<div class="search-button  active">
    @using (Html.BeginForm("Index", "Multiplexes", FormMethod.Get))
    {
        @Html.TextBox("SearchText")
        <input type="submit" value="Search" class="search-button" />
    }
</div>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Multiplex_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Movie_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Movie.Movie_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Multiplex_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Movie_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Movie.Movie_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Multiplex_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Multiplex_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Multiplex_id })
        </td>
    </tr>
}

</table>
